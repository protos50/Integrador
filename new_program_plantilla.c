/* Escribir una función generarVectorPares que genere un vector con los números pares comprendidos entre 1 y 100. Luego, escribir una función maximo que reciba un vector como
 parámetro y retorne el máximo elemento del arreglo. Probar la función maximo en la función principal, utilizando el vector de números pares generado anteriormente. */
#include <stdio.h>

void esperarIntro(void);


int main(void){	

	esperarIntro();
	return 0;
}

void esperarIntro(void){
	fflush(stdin);
	puts("\n\nPulsar Intro para continuar...");
	getchar();
}	
